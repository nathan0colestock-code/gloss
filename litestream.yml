dbs:
  - path: /app/data/foxed.db
    replicas:
      - type: s3
        endpoint: ${R2_ENDPOINT}
        bucket: your-r2-bucket
        path: gloss/gloss.db
        region: auto
        access-key-id: ${R2_ACCESS_KEY_ID}
        secret-access-key: ${R2_SECRET_ACCESS_KEY}
